"""
核心域（Core）

这一层是整个项目的心脏，包含所有业务逻辑。
最重要的规则：这一层的代码不能依赖任何外部技术（不能import第三方库）。
它只能依赖Python标准库和自己定义的接口。

子模块：
  - agent/       智能体核心（对话、规划、游戏决策）
  - memory/      记忆系统（短期记忆 + 长期记忆）
  - emotion/     情感引擎（情绪分析和状态管理）
  - personality/ 人格系统（White Salary独有的性格）
  - filter/      内容安全过滤
  - interfaces/  端口定义（所有抽象接口）
"""
